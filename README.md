# Image Search Abstraction Layer
## Node.js app , API and Microservices


<b> * NOTES : </b>

```
        - npm init ;
        - npm install ;
        - npm install express;
        -  npm install --save cors ;
        -  npm install --save body-parser ;
        - npm install --save mongoose ;
        -  npm install --save node-bing-api ;
        - RUN  nodemon ;
        - Go to browser: http://localhost:3000/api/imagesearch/test?offset=2 and check here to seee api strings;
        -  Run mongo in terminal to see mongo server;
        - http://localhost:3000/api/recentsearchs ;
        - http://localhost:3000/api/imagesearch/test ;
        - http://localhost:3000/api/imagesearch/pokemon/ ;
        
        
```
